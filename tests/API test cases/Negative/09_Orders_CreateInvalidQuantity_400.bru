meta {
  name: Orders - POST /api/orders invalid quantity
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/orders
  body: json
  auth: none
}

body:json {
  {
    "address": {
      "flatHomeDetails": "221B MG Road",
      "area": "Indiranagar",
      "landmark": "Near Metro",
      "pinCode": "560038",
      "city": "Bengaluru",
      "state": "Karnataka"
    },
    "items": [
      {
        "itemId": "ni-1",
        "quantity": 0
      }
    ]
  }
}

tests {
  test("status should be 400", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("error message should include invalid quantity", function() {
    const body = res.getBody();
    expect(body.message).to.equal("Invalid quantity for ni-1");
  });
}