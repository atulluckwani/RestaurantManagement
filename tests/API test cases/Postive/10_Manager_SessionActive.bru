meta {
  name: Manager - GET /api/manager/session active
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/manager/session
  body: none
  auth: none
}

tests {
  test("status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("session should be active", function() {
    const body = res.getBody();
    expect(body.active).to.equal(true);
  });
}