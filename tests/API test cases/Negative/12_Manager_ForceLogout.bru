meta {
  name: Manager - POST /api/manager/logout cleanup
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/manager/logout
  body: none
  auth: none
}

tests {
  test("status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}