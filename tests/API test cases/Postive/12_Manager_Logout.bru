meta {
  name: Manager - POST /api/manager/logout
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/manager/logout
  body: none
  auth: none
}

tests {
  test("status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("response should confirm logout", function() {
    const body = res.getBody();
    expect(body.message).to.equal("Logged out");
  });
}